const expect = require('chai').expect;
const temperature = require('./../src/javascripts/temperature');

describe('#closestToZero', function() {
  it('should return true when the value is true', () => {
      expect(true).to.be.true;
  });
});